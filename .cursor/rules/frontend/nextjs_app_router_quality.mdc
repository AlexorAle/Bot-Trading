---
description: "Next.js App Router: server components, DX, accesibilidad."
globs:
  - "frontend/**"
  - "apps/web/**"
  - "src/app/**"
alwaysApply: false
visibility: "auto"
examples:
  valid:
    - "Server components por defecto; `use client` solo si corresponde."
  invalid:
    - "Hooks del cliente en server components."
---
# Requisitos
- Metadatos en rutas; layouts claros.
- Tests mínimos en componentes con lógica.
- Accesibilidad básica (roles/labels/aria).
